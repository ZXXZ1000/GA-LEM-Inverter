# 代码说明

这个项目的核心目标是：用 FastScape 做地貌正演，用遗传算法搜索最合适的抬升场，并逐步接入 Pecube，让热年代学数据也能参与约束。

## 总入口

普通用户只需要看两个文件：

- `config.ini`：选择实验模式、输入数据、模型参数和输出目录。
- `runner.py`：统一运行入口。

运行命令：

```bash
python runner.py
```

程序启动后会读取：

```ini
[Run]
mode = main
```

然后根据 `mode` 选择不同实验流程。

## 通用输出结构

每次运行都会创建一个独立输出目录：

```text
demo/outputs/000X_时间_mode/
├── config_used.ini
├── summary.md
├── run_manifest.json
├── logs/
├── figures/
├── arrays/
├── metrics/
└── mode 专属输出目录
```

优先看 `summary.md`，它会记录本次实验模式、关键参数、主要指标和输出文件位置。

## main 模式：真实 DEM 反演

`main` 是真实 DEM 主优化反演流程。

数据流：

```text
demo/data/demo_dem.tif 或用户自己的 DEM
  -> DEM 读取和预处理
  -> 可选 fault_shp / study_area_shp 裁剪；填 none 时跳过
  -> 得到目标地形 target_topography
  -> GA 生成候选 uplift field
  -> FastScape 正演生成 simulated_topography
  -> 比较 simulated_topography 和 target_topography
  -> 得到 fitness/loss
  -> GA 迭代搜索 best uplift
  -> 输出 best uplift、重建地形、误差图和 metrics
```

这个模式当前的核心约束是：让 FastScape 正演出来的现代地形尽量接近输入 DEM。

## synthetic 模式：合成验证

`synthetic` 用来验证算法链路本身是否可靠。

数据流：

```text
config.ini 里的 synthetic 参数
  -> 生成一个已知 true_uplift
  -> FastScape 用 true_uplift 生成 synthetic target terrain
  -> GA 只看到 synthetic target terrain
  -> GA 反推 uplift
  -> 对比 recovered uplift 和 true_uplift
  -> 输出验证图和指标
```

这个模式回答的问题是：如果真值是已知的，这套 GA + FastScape 是否能把它找回来。

## k_sensitivity 模式：K/scale_factor 敏感性实验

`k_sensitivity` 用来比较不同 `scale_factor/K` 对结果和速度的影响。

数据流：

```text
一组 scale_factor/K
  -> 对每个 K 跑一遍验证实验
  -> 比较不同 K 下的地形误差、uplift 误差和耗时
  -> 输出对比表和图
```

它主要回答：抬升场降维到多粗才合适。

## pecube_coupled 模式：FastScape 到 Pecube 的耦合验证

`pecube_coupled` 是新增的 FastScape + Pecube 耦合 smoke 流程。

它现在的目的不是正式优化，而是验证：FastScape 的时序地形和抬升场能否被转换成 Pecube 项目，并真实跑出热年代学预测结果。

数据流：

```text
config.ini
  -> mode = pecube_coupled
  -> 生成轻量 uplift/topography/temperature 序列
  -> FastScape 生成 topography_series
  -> PecubeProjectBuilder 写 Pecube project

pecube/PGB01/
├── input/Pecube.in
└── data/fastscape/
    ├── topo0, topo1 ...
    ├── uplift0, uplift1 ...
    └── temp0, temp1 ...

  -> PecubeEngine 调 vendor/pecube/bin/Test
  -> PecubeEngine 调 vendor/pecube/bin/Pecube
  -> PecubeOutputParser 读取 output/*.csv
  -> 输出 pecube_result.json 和 pecube_metrics.json
```

Pecube 的输出示例：

```text
pecube/PGB01/output/
├── Ages001.csv
├── Ages.out
└── Pecube.out
```

## Pecube 相关代码放在哪里

Pecube 上游源码放在：

```text
vendor/pecube/source/
```

编译后的可执行文件放在：

```text
vendor/pecube/bin/
```

Python 胶合层放在：

```text
ga_lem_inverter/integrations/
├── pecube.py
├── pecube_project.py
├── pecube_parser.py
└── pecube_loss.py
```

runner 的 Pecube 工作流放在：

```text
ga_lem_inverter/workflows/pecube_coupled.py
```

## 当前已经完成的闭环

现在已经能跑通：

```text
FastScape sequence
  -> Pecube project
  -> Pecube Test
  -> Pecube run
  -> parsed output
```

也就是说，Pecube 已经可以作为 FastScape 后处理链路运行。

## 下一步真正的科学闭环

当前 `main` 模式的优化目标主要是 DEM 地形约束。Pecube 接入后，下一步应该把热年代学约束并入遗传算法 fitness。

未来完整流程应该是：

```text
GA candidate uplift
  -> FastScape 正演现代地形
  -> terrain_loss = simulated_topography vs observed_dem
  -> 同时把 FastScape topography/uplift history 交给 Pecube
  -> Pecube 输出 predicted thermochronology ages / thermal histories
  -> thermo_loss = predicted ages vs observed samples
  -> total_fitness = terrain_loss + thermo_loss
  -> GA 继续搜索
```

一句话总结：现在主反演已经是 DEM 约束的 uplift 搜索；Pecube 现在已经接成可运行的 FastScape 后处理链路，但还没有正式并入 GA fitness。
